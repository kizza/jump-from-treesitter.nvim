" Author: kizza <hello@keiranoleary.com>
" Description: A jump to definition tool for nvim using treesitter

" Fallback to execute when no match is found
" let g:jump_from_treesitter_fallback = "call CocAction('jumpDefinition')"
